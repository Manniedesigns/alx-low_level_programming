#include<stdio.h>

/**
 * main - Entry point
 *
 * Description: prints lowercase letters
 *
 * Return: Always returns 0 (Success)
 */

int main(void)
{
	char a;

	for (a = 'a'; a <= 'z'; a++)
		putchar('a');
	putchar('\n');
	return (0);
}
